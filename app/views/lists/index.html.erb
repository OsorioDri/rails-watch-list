<div class="container">
  <h1>My Lists</h1>
  <ul>
  <% @lists.each do |list| %>
    <li>
    <%= list.id %>
    <%= link_to "#{list.name}", list %>
    </li>
  <% end %>
  </ul>

  <%= link_to 'Add a list', new_list_path %> |
</div>
